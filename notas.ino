/*if (distancia <= 9) {
    parar();
    delay(500);
    Mov90GrausDireita();
    parar();
    delay(500);
    frente(58, 63);
    delay(500);
    parar();
    delay(500);
    Mov90GrausEsquerda();
    parar();
    delay(500);
    frente(64, 63);
    delay(700);
    parar();
    delay(500);
    Mov90GrausEsquerda();
    parar();
    delay(500);
    frente(60, 63);
    delay(700);
    parar();
    delay(500);
    Mov90GrausDireita();
  }*/
/*/Detecta a cor
  detectaCor();
  detectaCorB();
  //Mostra valores no serial monitor
  // Serial.print("Vermelho :");
  // Serial.print(valorVermelho);

  // Serial.print(" Verde : ");
  // Serial.print(valorVerde);

  // Serial.print(" Azul : ");
  // Serial.print(valorAzul);

  // Serial.print(" Branco : ");
  // Serial.print(valorBranco);
  // Serial.println();

  //Verifica se a cor vermelha foi detectada
  if ((valorVermelho < valorAzul) &&
      (valorVermelho < valorVerde) &&
      (valorBranco < 100)) {
    Serial.println("Vermelho");


  } else if ((valorAzul < valorVermelho) &&  //Verifica se a cor azul foi detectada
             (valorAzul < valorVerde) &&
             (valorBranco < 100)) {
    Serial.println("Azul");


  } else if ((valorVerde < valorVermelho) &&  //Verifica se a cor verde foi detectada
             (valorVerde < valorAzul) &&
             (valorBranco < 100)) {
    Serial.println("Verde");

  }
  Serial.println();

  if ((valorVermelhoB < valorAzulB) &&
      (valorVermelhoB < valorVerdeB) &&
      (valorBrancoB < 100)) {
    Serial.println("Vermelho");


  } else if ((valorAzulB < valorVermelhoB) &&  //Verifica se a cor azul foi detectada
             (valorAzulB < valorVerdeB) &&
             (valorBrancoB < 100)) {
    Serial.println("Azul");


  } else if ((valorVerdeB < valorVermelhoB) &&  //Verifica se a cor verde foi detectada
             (valorVerdeB < valorAzulB) &&
             (valorBrancoB < 100)) {
    Serial.println("Verde");

  }
  Serial.println();
*/